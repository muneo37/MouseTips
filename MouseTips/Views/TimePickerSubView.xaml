<Window
    x:Class="MouseTips.Views.TimePickerSubView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MouseTips.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="TimePickerSubView"
    Width="400"
    Height="200"
    AllowsTransparency="True"
    WindowStartupLocation="Manual"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <local:CirculationScroll x:Name="hourScroll" Grid.Column="0"/>
            <local:CirculationScroll x:Name="minuteScroll" Grid.Column="1"/>
            <local:CirculationScroll x:Name="ampmScroll" Grid.Column="2"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="0"
                Click="OnOK"
                Content="OK" />
            <Button
                Grid.Column="1"
                Click="OnCancel"
                Content="キャンセル" />
        </Grid>
    </Grid>
</Window>
